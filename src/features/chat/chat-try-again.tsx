import { Button } from "@/components/ui/button";
import { RefreshCcw } from "lucide-react";

export const ChatTryAgain = () => {
  return (
    <Button size="icon" variant="ghost">
      <RefreshCcw />
    </Button>
  );
};
