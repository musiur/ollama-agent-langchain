import LoginPage from "@/features/auth/login/login-page";

/**
 * LoginPage
 * @returns {React.ReactNode}
 * @description This is the page for the login where the main content is located
 */
const Page = (): React.ReactNode => {
  return <LoginPage />;
};

export default Page;
