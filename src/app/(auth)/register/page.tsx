import RegisterPage from "@/features/auth/register/register-page";

/**
 * RegisterPage
 * @returns {React.ReactNode}
 * @description This is the page for the register where the main content is located
 */
const Page = (): React.ReactNode => {
  return <RegisterPage />;
};

export default Page;
